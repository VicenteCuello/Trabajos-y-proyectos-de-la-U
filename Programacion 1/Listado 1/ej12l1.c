#include <stdio.h>    //solo, nombre de usuario timus es Vicente Cuello
#include <stdlib.h>

int main() {
  int num;
  scanf("%d",&num);
  if(7<=num && num<=12){
    printf("YES");
  }
  else{
    printf("NO");
  }


  return 0;
}